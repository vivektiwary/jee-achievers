# Seo support

# Review support (someone should be able to add review on the site and stored in github repo or cloud [whichever is each to implement])

# Sharable link for asking testimonials /review, when they submit, it should be stored in github repo or cloud [whichever is each to implement]

# Contact form support (when they submit, it should be stored in github repo or cloud [whichever is each to implement])
